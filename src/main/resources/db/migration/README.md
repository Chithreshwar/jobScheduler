# Flyway Migrations

Place your versioned SQL migration scripts in this directory, following the
`V__description.sql` naming convention (for example, `V1__init_schema.sql`).

No migrations have been defined yet.

